#include<stdio.h>
#include<stdlib.h>
#include<time.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/

int main(void)
{
int ch = 97;
for (ch = 97; ch < 123; ch++)
putchar(ch);

return (0);
}

